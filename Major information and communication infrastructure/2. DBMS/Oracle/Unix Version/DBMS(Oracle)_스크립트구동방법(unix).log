1> 서버에 root로 접속 (or 다른 계정 접속 후 su 로 관리자 권한으로 변경)
    # su -

2> 루트 디렉토리(최상위 디렉토리)에think 디렉토리 생성
    # mkdir /think

3> think 디렉토리 권한 변경
    # chmod 777 /think

4> think 디렉토리 이동
    # cd /think

5> think 폴더에 스크립트 업로드(3개 파일)

6> 스크립트 실행 파일 권한 변경
   # chmod 777 (3개 파일)

7> oracle계정으로 변경
   # su - oracle 

8> think디렉토리로 경로 이동
   # cd /think

9> oracle 홈 디렉토리 경로 확인
   # set | grep -i oracle_home

10> DB쿼리를 위한 sqlplus 실행
   # sqlplus "/as sysdba"

11> ORACLE 진단 스크립트 실행
   SQL> @oracle_unix.sql 

12> 결과파일 다운로드 및 전달
   결과파일 예시) hostname_날짜_oracle_인스턴스명.txt

13> 모든작업 완료 후 think 폴더 및 스크립트 삭제 
